package com.solbox.delivery.vm;

import org.json.JSONArray;
import org.json.JSONException;
import org.json.JSONObject;

public class Facilities {

	static void deleteAllVM(String jsonString, String token) throws Exception {
		
		JSONObject jsonObject = new JSONObject(jsonString);
		JSONArray servers = jsonObject.getJSONArray("servers");
		
		for(int i=0; i<servers.length();i++ ) {
			JSONObject VM = servers.getJSONObject(i);
			String VM_ID = VM.getString("id");
			Utils.request(App.forceDeleteVMURL, App.POST, token, App.USER_AGENT, App.CONTENT_TYPE, RequestBody.forceDeleteVM(), VM_ID);
		}			
	}
}
